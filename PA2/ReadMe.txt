Program assignment 2:
 
Client using customized protocol on top of UDP protocol for requesting
identification from server for access permission to the network.

- Run the below commands to compile the C programs in the terminal after navigating using cd command to the destination folder:
	gcc Server.c -o server
	gcc Client.c -o client
- Start the server,run: ./server
- In a new terminal window,run the client program: ./client
