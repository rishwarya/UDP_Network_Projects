Program assignment 1:
 
Client using customized protocol on top of UDP protocol for sending
information to the server.

- Run the below commands to compile the C programs in the terminal after navigating using cd command to the destination folder:
	gcc Server.c -o server
	gcc Client.c -o client
- Start the server : ./server
- In a new terminal window, run the client : ./client
